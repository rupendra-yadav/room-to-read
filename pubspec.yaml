name: room_to_read
description: A new Flutter project

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.10.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  connectivity_plus: ^5.0.2
  get: ^4.7.3
  http: ^1.6.0
  intl: ^0.20.2
  path: ^1.9.1
  shared_preferences: ^2.5.4
  shimmer: ^3.0.0
  sqflite: ^2.4.2
  uuid: ^4.5.2

  excel: ^4.0.6
  path_provider: ^2.1.2
  share_plus: ^10.0.0  # for sharing/saving the file

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
